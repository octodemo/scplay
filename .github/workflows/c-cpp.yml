name: C/C++ CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      
    - name: Install tools
      run: |
        sudo apt-get update
        sudo apt-get install -qq qt5-default
        qmake --version
        
    - name: qmake
      run: qmake
      
    - name: make
      run: make
      
